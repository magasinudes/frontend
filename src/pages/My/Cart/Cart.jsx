import React from "react";
import { AppContainer, AppContent, AppMain } from "$common";

const Cart = () => (
  <AppContainer>
    <AppContent>
      <AppMain>
        <div>Cart</div>
      </AppMain>
    </AppContent>
  </AppContainer>
);

export default Cart;
