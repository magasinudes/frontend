import React from "react";
import { AppContainer, AppContent, AppMain } from "$common";

const Orders = () => (
  <AppContainer>
    <AppContent>
      <AppMain>
        <div>Orders</div>
      </AppMain>
    </AppContent>
  </AppContainer>
);

export default Orders;
