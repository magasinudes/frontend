import React from "react";
import { AppContainer, AppContent, AppMain } from "$common";

const Dashboard = () => (
  <AppContainer>
    <AppContent>
      <AppMain>
        <div>Dashboard</div>
      </AppMain>
    </AppContent>
  </AppContainer>
);

export default Dashboard;
