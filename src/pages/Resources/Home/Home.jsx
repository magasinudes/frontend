import React from "react";
import { AppContainer, AppContent, AppMain } from "$common";

const Home = () => (
  <AppContainer>
    <AppContent>
      <AppMain>
        <div>Resources Index</div>
      </AppMain>
    </AppContent>
  </AppContainer>
);

export default Home;
